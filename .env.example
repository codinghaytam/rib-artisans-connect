# Copy this file to .env and fill in your project-specific values
# Note: Only variables prefixed with VITE_ are exposed to the browser (Vite).
# Backend/Edge Function secrets must be set in Supabase project secrets, not committed.

# --- Frontend (Vite) ---
# Used by the React app when creating the Supabase client
VITE_SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="YOUR_SUPABASE_ANON_KEY"
# Optional helper (project ref is the subdomain part of your Supabase URL)
VITE_SUPABASE_PROJECT_ID="YOUR_PROJECT_REF"

# Third-party APIs used in the frontend
# Geoapify is used for autocomplete & reverse geocoding in LocationSearch
VITE_GEOAPIFY_API_KEY="YOUR_GEOAPIFY_API_KEY"

# Storage bucket used for all image uploads (avatars, portfolio, etc.)
VITE_SUPABASE_STORAGE_BUCKET="public"

# --- Backend/Edge Functions & Local Scripts ---
# These are NOT loaded by Vite. Set them as environment variables
# (e.g., export in your shell) and as Supabase Function secrets.

# Base URL of your Supabase project (needed by Edge Functions and scripts)
SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"

# Public anon key (Edge Functions that act as a public client)
# Used by: supabase/functions/send-contact-email, supabase/functions/track-view
SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# Service role key (admin privileges) — keep this secret
# Used by: supabase/functions/process-application, create-notification, seed-demo-accounts
# and scripts/load-mock-data.sh
SUPABASE_SERVICE_ROLE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY"

# --- Optional: Migration script helpers (scripts/migrate-backend.ts) ---
# If you use the migration script to copy data between projects, fill these in.
OLD_SUPABASE_URL="https://OLD_PROJECT_REF.supabase.co"
OLD_SUPABASE_KEY="OLD_SERVICE_ROLE_KEY"
NEW_SUPABASE_URL="https://NEW_PROJECT_REF.supabase.co"
NEW_SUPABASE_KEY="NEW_SERVICE_ROLE_KEY"

# Tips
# - For local scripts (like scripts/load-mock-data.sh), export vars in your shell or `source .env`.
# - For Supabase Edge Functions, set secrets via Dashboard (Project Settings > Functions) or CLI:
#     supabase functions secrets set SUPABASE_URL="..." SUPABASE_ANON_KEY="..." SUPABASE_SERVICE_ROLE_KEY="..."
# - Never commit real keys to version control.
